---
description: Legacy slash-entry shim for the humanize-write skill. Prefer the skill directly.
---

# Humanize Write Command (Shim)

Use this to invoke the `humanize-write` skill via slash command. The maintained workflow lives in `skills/humanize-write/SKILL.md`.

## Arguments

`$ARGUMENTS`

## Delegation

Apply the `humanize-write` skill using the provided arguments.
